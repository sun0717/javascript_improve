/* const pet = function (name) {
    const getName = function() {
        // 内部函数可以访问外部函数的 "name" 变量
        return name;
    }
    return getName;
}

const myPet = pet('mocy');

console.log(myPet()); */

// 2
/* const getCode = (function () {
    const apiCode = "01234567";

    return function() {
        return apiCode;
    }
})();
console.log(getCode()); */
function cat() {
    let name = '';
    
}